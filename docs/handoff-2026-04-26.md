# Session handoff — 2026-04-26

Tight reorientation doc for picking this back up in terminal. Read top to bottom.

---

## State

- **Branch:** `claude/integrate-claude-design-BE9tL`
- **Tracks:** `origin/claude/integrate-claude-design-BE9tL` (Denis's fork)
- **Base:** `aad01e9` (Dan's `upstream/main`, fully synced)
- **HEAD:** `81c9cd3`
- **Working tree:** clean. Everything pushed.
- **Upstream remote:** `https://github.com/PGD3311/What-s-Good-Here.git` configured as `upstream`.

---

## What shipped this session

In commit order (most recent first):

| SHA | Topic |
|---|---|
| `81c9cd3` | Tier-1 editorial atoms behind `/redesign-atoms` preview route |
| `f3512bb` | Drop claude.ai/design handoff bundle at `/wgh-redesign` (static) |
| `082aaa1` | `docs/tone-and-voice.md` — strategic note for Dan |
| `6883186` | Empty-state category suggestions in DishSearch dropdown |
| `9769d8f` | Unify search bars (dishes + restaurants + Places + Add new everywhere) |

Earlier in the session: previous redesign work (Masthead, zine theme, type system) was reset/abandoned per Dan-alignment direction. Recoverable from `origin/archive/claude-redesign-2026-04-26`.

---

## Preview URLs (Vercel preview for this branch)

| Path | What |
|---|---|
| `/wgh-redesign` | Static prototype from claude.ai/design — full iOS-frame walkthrough w/ Tweaks panel (CSP scoped to allow unpkg + Babel-Standalone). |
| `/redesign-atoms` | React port of editorial atoms — Rating, RankNum, WorthBadge, DeltaTag, DishThumb, SectionHead, VoteStamp + composed dish row + dotted-leader menu. Hidden, no nav link. |
| `/` | Live app, unchanged. |

---

## Open decisions, parked on Dan

The redesign integration is **not actionable yet**. Dan owns the brand call. Three things waiting for his reaction:

1. **`docs/tone-and-voice.md`** — should WGH lean more authoritative? Includes 12-row copy rewrite table, type/color recommendations, what we lose, what we gain, phasing.
2. **`/wgh-redesign`** — full static design walkthrough. Editorial direction (Fraunces + Geist + JetBrains Mono), Honesty Index banner on Profile, Worth-it/Skip stamps, dotted-leader menu rows, delta-pill (your rating vs crowd).
3. **`/redesign-atoms`** — proof the atoms work in the live build environment.

---

## Search bar work — already shipped, no Dan blocker

These two changes (`9769d8f`, `6883186`) are non-redesign UX improvements ready to ship to production whenever:

- Every globally-positioned search bar (Home, Map, Restaurants, Locals) now surfaces dishes + restaurants + Google Places "Add new" through the unified DishSearch dropdown.
- Empty-state suggestions panel: focused empty search bar shows 8 popular categories ("Try a category" eyebrow + chips) instead of nothing.

Path to production: PR `claude/integrate-claude-design-BE9tL` → `PGD3311/What-s-Good-Here:main`. Branch protection requires Dan review. **Not opened yet** — needs user-driven PR creation since MCP scope is `denisgingras75/wgh` only.

---

## Next concrete moves if greenlit

In the order I'd do them:

1. **Open the PR** to `upstream/main` for the search work. That's the cleanest, lowest-risk thing to ship. Dan reviews, merges, prod deploys.
2. **Have the tone conversation with Dan**, sending him the three preview URLs + the doc. Wait for his reaction before any visual port.
3. **If Dan greenlights authoritative tone:**
   - Tier 2: replace existing `Masthead` / `HomeListMode` brand header with a `SectionHead`-built editorial header. ~1 day.
   - Profile Honesty banner — most distinctive feature, no schema (`would_again` derives from `rating ≥ 7` until proper column lands). ~1 day.
   - Editorial DishListItem ranked variant — touches Home + Browse + Restaurant Detail at once. ~1 day.
4. **Tier 3 (post-launch):** `would_again` boolean column on `votes`, `percent_worth_it` aggregation in RPCs, real Honesty Index metrics.

---

## Don't do without Dan

- Touch any user-facing surface to apply the editorial atoms. Currently they're behind `/redesign-atoms` for a reason.
- Replace 1–10 rating with letters / pairwise / binary. Long discussion this session ended with: leave the rating mechanic alone, fix display surfaces first. (Letter-grade prototype was built and deleted; lives at `archive/claude-redesign-2026-04-26` if you ever want to dig it up.)
- Add a "vibes slider" or similar restaurant-level scoring. Out of scope until rating system question is settled.
- Force-push `main` on either Denis's fork or Dan's repo.

---

## Useful files to re-read on resume

- `CLAUDE.md` — non-negotiables, project conventions, role guidance
- `CURRENT_FOCUS.md` — collision-prevention handoff (cleared at session end)
- `docs/tone-and-voice.md` — strategic note for Dan
- `src/components/editorial-atoms.jsx` — the new atoms
- `src/pages/RedesignAtoms.jsx` — atoms gallery
- `public/wgh-redesign/` — full prototype source (read README.md inside the bundle if Dan greenlights deeper port)

---

## Branches you should know about

- `claude/integrate-claude-design-BE9tL` — current working branch
- `upstream/main` — Dan's main, synced
- `origin/main` — Denis's fork main (1 commit ahead of `upstream/main`: Value Badge feature)
- `origin/archive/claude-redesign-2026-04-26` — abandoned aggressive redesign (Masthead, zine theme, full type-system port)
- `origin/redesign/*` — older redesign experiments, ignore

---

*Session ended ~04:25 UTC. No active in-flight surface — handoff is clean.*
